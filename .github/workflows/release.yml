# NekoBeat multi-platform release
# Docs: https://v2.tauri.app/distribute/pipelines/github/
#
# Triggers: tag v* or Actions → Release → Run workflow
#
# Desktop: Windows / Linux / macOS arm64 + Intel / Android
#
# Updater signing (createUpdaterArtifacts: true in tauri.conf.json):
#   TAURI_SIGNING_PRIVATE_KEY          = contents of .tauri/nekobeat.key
#   TAURI_SIGNING_PRIVATE_KEY_PASSWORD = password used with `tauri signer generate -p`
#   (Password is REQUIRED in CI — passwordless keys fail non-interactively.)
#
# Android APK signing (sideload / Play):
#   ANDROID_KEYSTORE_BASE64, ANDROID_KEYSTORE_PASSWORD, ANDROID_KEY_ALIAS, ANDROID_KEY_PASSWORD
#   If unset, CI generates an ephemeral keystore (uninstall old APK between runs).

name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      draft:
        description: 'Create draft GitHub release'
        type: boolean
        default: true
      build_android:
        description: 'Also build Android APK'
        type: boolean
        default: true

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # One draft release first so every platform uploads to the same tag/page.
  prepare-release:
    runs-on: ubuntu-22.04
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      release_id: ${{ steps.meta.outputs.release_id }}
      version: ${{ steps.meta.outputs.version }}
      release_name: ${{ steps.meta.outputs.release_name }}
      draft: ${{ steps.meta.outputs.draft }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create or reuse single GitHub Release
        id: meta
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REF_TYPE: ${{ github.ref_type }}
          REF_NAME: ${{ github.ref_name }}
          # Boolean footgun: `x && draft || true` is always true when draft=false
          WANT_DRAFT: ${{ github.event_name != 'workflow_dispatch' || inputs.draft }}
        run: |
          set -euo pipefail
          VERSION="$(python3 -c "import json; print(json.load(open('package.json'))['version'])")"
          if [ "$REF_TYPE" = "tag" ]; then
            TAG="$REF_NAME"
          else
            TAG="app-v${VERSION}"
          fi
          RELEASE_NAME="NekoBeat v${VERSION}"
          if [ "${WANT_DRAFT}" = "true" ]; then
            DRAFT=true
          else
            DRAFT=false
          fi

          # Keep every line indented under run:| so YAML does not treat markdown
          # tables ("| ... |") as a new block scalar. Strip the indent for notes.
          NOTES_FILE="${RUNNER_TEMP}/nekobeat-release-notes.md"
          cat > "$NOTES_FILE" <<'EOF'
          ## NekoBeat

          ### Desktop
          Full app: Browse, Listen, Library, streaming (YouTube / SoundCloud / Spotify), EQ, lyrics, updater.

          | Platform | Artifacts |
          |----------|-----------|
          | Windows | `.msi` / NSIS `.exe` |
          | Linux | `.deb` / `.AppImage` |
          | macOS | `.dmg` (Apple Silicon + Intel) |

          ### Android (library preview)
          APK: `NekoBeat-*-android-*.apk`

          This build is for **music already on your phone** — Library scan/play, offline Liked files, Settings, EQ.
          Search and online streaming are **off** until they work as well as desktop.

          Uninstall any older APK before installing.
          EOF
          sed -i 's/^          //' "$NOTES_FILE"

          # Resolve release id without /releases/tags/{tag} — that 404s for
          # draft "untagged-*" releases (common after workflow_dispatch).
          resolve_release_id() {
            local id=""
            id="$(gh release view "$TAG" --json databaseId --jq .databaseId 2>/dev/null || true)"
            if [ -z "$id" ]; then
              id="$(gh api "repos/${GITHUB_REPOSITORY}/releases" --paginate \
                --jq ".[] | select(.tag_name == \"${TAG}\" or .name == \"${RELEASE_NAME}\") | .id" \
                | head -n1 || true)"
            fi
            printf '%s' "$id"
          }

          EXISTING_ID="$(resolve_release_id || true)"
          if [ -n "${EXISTING_ID}" ]; then
            echo "Reusing existing release id=${EXISTING_ID} tag=${TAG}"
            # Bind a real git tag to this commit so later uploads /tag/ lookups work
            EDIT_ARGS=(--title "$RELEASE_NAME" --notes-file "$NOTES_FILE" --tag "$TAG" --target "$GITHUB_SHA")
            if [ "$DRAFT" = "true" ]; then
              EDIT_ARGS+=(--draft)
            else
              EDIT_ARGS+=(--draft=false)
            fi
            # Prefer edit by numeric id (works for untagged-xxx drafts)
            gh release edit "$EXISTING_ID" "${EDIT_ARGS[@]}" || gh release edit "$TAG" "${EDIT_ARGS[@]}"
          else
            echo "Creating release tag=$TAG draft=$DRAFT target=$GITHUB_SHA"
            CREATE_ARGS=(--title "$RELEASE_NAME" --notes-file "$NOTES_FILE" --target "$GITHUB_SHA")
            if [ "$DRAFT" = "true" ]; then
              CREATE_ARGS+=(--draft)
            fi
            gh release create "$TAG" "${CREATE_ARGS[@]}"
          fi

          RELEASE_ID="$(resolve_release_id)"
          if [ -z "$RELEASE_ID" ]; then
            echo "::error::Could not resolve GitHub release id for tag=${TAG}"
            gh release list --limit 20 || true
            exit 1
          fi

          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "release_id=${RELEASE_ID}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "release_name=${RELEASE_NAME}" >> "$GITHUB_OUTPUT"
          echo "draft=${DRAFT}" >> "$GITHUB_OUTPUT"
          echo "Prepared release id=${RELEASE_ID} tag=${TAG} name=${RELEASE_NAME}"

  publish-desktop:
    needs: prepare-release
    strategy:
      fail-fast: false
      matrix:
        include:
          # Apple Silicon (native on macos-latest)
          - platform: macos-latest
            args: >-
              --config src-tauri/tauri.desktop.conf.json
              --config src-tauri/tauri.macos.conf.json
              --target aarch64-apple-darwin
            rust_targets: aarch64-apple-darwin
          # Intel Mac: native x86_64 on macos-15-intel (macos-13 retired Dec 2025)
          # Do NOT cross-compile from arm — glib-sys/pkg-config breaks
          - platform: macos-15-intel
            args: >-
              --config src-tauri/tauri.desktop.conf.json
              --config src-tauri/tauri.macos.conf.json
              --target x86_64-apple-darwin
            rust_targets: x86_64-apple-darwin
          - platform: ubuntu-22.04
            args: >-
              --config src-tauri/tauri.desktop.conf.json
              --config src-tauri/tauri.linux.conf.json
            rust_targets: ''
          - platform: windows-latest
            args: >-
              --config src-tauri/tauri.desktop.conf.json
              --config src-tauri/tauri.windows.conf.json
            rust_targets: ''

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_targets }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Prepare updater signing env
        shell: bash
        env:
          KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          PW: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: |
          if [ -z "$KEY" ]; then
            echo "::error::Missing secret TAURI_SIGNING_PRIVATE_KEY (paste entire .tauri/nekobeat.key)."
            exit 1
          fi
          if [ -z "$PW" ]; then
            echo "::error::Missing secret TAURI_SIGNING_PRIVATE_KEY_PASSWORD."
            exit 1
          fi
          KEY_FILE="${RUNNER_TEMP}/tauri_updater.key"
          export KEY_FILE
          # Local key is a single base64 line (len 348). A trailing \n becomes
          # "Invalid symbol 10, offset 348" — do NOT append a newline.
          python3 - <<'PY'
          import os, re
          from pathlib import Path
          raw = os.environ["KEY"].replace("\r\n", "\n").replace("\r", "\n")
          body = "\n".join(
              line for line in raw.split("\n")
              if line and not line.startswith("untrusted comment:")
          )
          # Keep only base64 alphabet (drops paste newlines/spaces)
          payload = re.sub(rb"[^A-Za-z0-9+/=]", b"", body.encode("utf-8"))
          if not payload:
              raise SystemExit("private key base64 body is empty after sanitize")
          if b"\n" in payload or 10 in payload:
              raise SystemExit("sanitize failed: newline still in payload")
          # Write EXACT bytes — no trailing newline (offset 348 == former bug)
          Path(os.environ["KEY_FILE"]).write_bytes(payload)
          print(f"updater key file ready: base64_len={len(payload)} trailing_nl=False")
          PY
          # tauri build reads TAURI_SIGNING_PRIVATE_KEY as path or contents
          echo "TAURI_SIGNING_PRIVATE_KEY=${KEY_FILE}" >> "$GITHUB_ENV"
          echo "TAURI_SIGNING_PRIVATE_KEY_PASSWORD=${PW}" >> "$GITHUB_ENV"

      - name: Linux system deps (WebKit)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libxdo-dev \
            libssl-dev \
            libunwind-dev \
            libasound2-dev \
            build-essential \
            curl wget file

      - name: Fetch yt-dlp + chmod sidecars (Linux / macOS)
        if: matrix.platform != 'windows-latest'
        run: |
          mkdir -p src-tauri/bin
          OS=$(uname -s)
          ARCH=$(uname -m)
          if [ "$OS" = "Darwin" ]; then
            curl -fsSL --retry 3 -o src-tauri/bin/yt-dlp \
              https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp_macos
          else
            curl -fsSL --retry 3 -o src-tauri/bin/yt-dlp \
              https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp_linux
          fi
          chmod +x src-tauri/bin/yt-dlp
          chmod +x src-tauri/binaries/spotiflac-cli-* || true
          # Fail early if platform sidecar missing (Tauri externalBin)
          case "$OS-$ARCH" in
            Darwin-arm64)
              test -f src-tauri/binaries/spotiflac-cli-aarch64-apple-darwin
              ;;
            Darwin-x86_64)
              test -f src-tauri/binaries/spotiflac-cli-x86_64-apple-darwin
              ;;
            Linux-*)
              test -f src-tauri/binaries/spotiflac-cli-x86_64-unknown-linux-gnu
              ;;
          esac

      - name: Ensure Windows yt-dlp
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          if (-not (Test-Path "src-tauri/bin/yt-dlp.exe")) {
            curl.exe -fsSL --retry 3 -o src-tauri/bin/yt-dlp.exe `
              https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp.exe
          }
          if (-not (Test-Path "src-tauri/binaries/spotiflac-cli-x86_64-pc-windows-msvc.exe")) {
            throw "Missing Windows spotiflac-cli sidecar in src-tauri/binaries/"
          }

      - name: Install frontend deps
        run: npm ci

      - name: Build & upload (tauri-action)
        uses: tauri-apps/tauri-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # TAURI_SIGNING_* already injected via GITHUB_ENV (password omitted when unset)
        with:
          # Pin every matrix OS to the same release page
          releaseId: ${{ needs.prepare-release.outputs.release_id }}
          tagName: ${{ needs.prepare-release.outputs.tag }}
          releaseName: ${{ needs.prepare-release.outputs.release_name }}
          releaseDraft: ${{ needs.prepare-release.outputs.draft == 'true' }}
          prerelease: false
          # Upload to uploads.github.com often flakes on large bundles
          retryAttempts: 5
          uploadWorkflowArtifacts: true
          args: ${{ matrix.args }}

  publish-android:
    # Attach APK to the same release as desktop (never create a second page).
    # always() so one failed desktop platform does not skip the APK.
    if: |
      always() &&
      !cancelled() &&
      needs.prepare-release.result == 'success' &&
      (github.event_name != 'workflow_dispatch' || inputs.build_android)
    runs-on: ubuntu-22.04
    continue-on-error: true
    needs:
      - prepare-release
      - publish-desktop

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install NDK + platform
        run: |
          # Must match android.ndkVersion that Tauri/AGP resolve (CXX1104 if ndk.dir disagrees)
          sdkmanager --install "ndk;27.0.12077973" "platforms;android-34" "build-tools;34.0.0"
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/27.0.12077973" >> "$GITHUB_ENV"
          echo "NDK_HOME=$ANDROID_HOME/ndk/27.0.12077973" >> "$GITHUB_ENV"
          echo "$ANDROID_HOME/ndk/27.0.12077973/toolchains/llvm/prebuilt/linux-x86_64/bin" >> "$GITHUB_PATH"

      - name: Install Rust + Android targets
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - name: Host build helpers
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config unzip curl xz-utils

      - name: Configure Android SDK path
        run: |
          set -euo pipefail
          mkdir -p src-tauri/gen/android
          echo "sdk.dir=$ANDROID_HOME" > src-tauri/gen/android/local.properties
          sed -i '/^gstAndroidRoot=/d' src-tauri/gen/android/gradle.properties || true

      - name: Install frontend deps
        run: npm ci

      - name: Ensure Android project (gen/)
        run: |
          if [ ! -f src-tauri/gen/android/app/build.gradle.kts ]; then
            echo "::error::src-tauri/gen/android is missing. Commit the patched Media3 Android project."
            exit 1
          fi

      - name: Sync Android launcher icons (brand)
        run: bash scripts/sync-android-icons.sh

      - name: Configure Android release signing
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: bash scripts/ci-android-signing.sh

      - name: Build Android APK (aarch64)
        env:
          PKG_CONFIG_SYSROOT_DIR: ''
          PKG_CONFIG_PATH: ''
        run: npm run tauri:android:build

      - name: Verify APK is signed (not *-unsigned)
        run: |
          set -euo pipefail
          mapfile -t APKS < <(find src-tauri/gen/android/app/build/outputs/apk -name '*.apk' -type f | sort)
          if [[ ${#APKS[@]} -eq 0 ]]; then
            echo "::error::No APK produced"
            exit 1
          fi
          for apk in "${APKS[@]}"; do
            echo "APK: $apk"
            if [[ "$apk" == *unsigned* ]]; then
              echo "::error::Unsigned APK still produced: $apk"
              exit 1
            fi
            "$ANDROID_HOME/build-tools/34.0.0/apksigner" verify --verbose "$apk" || \
              "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose "$apk"
            bash scripts/verify-android-media3-apk.sh "$apk"
          done

      - name: Stage APK into release/ (same folder as desktop assets)
        id: stage_apk
        run: |
          set -euo pipefail
          VERSION="${{ needs.prepare-release.outputs.version }}"
          TAG="${{ needs.prepare-release.outputs.tag }}"
          mkdir -p release
          mapfile -t APKS < <(find src-tauri/gen/android/app/build/outputs/apk -name '*-release*.apk' -type f ! -name '*unsigned*' | sort)
          if [[ ${#APKS[@]} -eq 0 ]]; then
            echo "::error::No signed release APK to stage"
            exit 1
          fi
          for apk in "${APKS[@]}"; do
            base="$(basename "$apk")"
            # Prefer abi from path: .../apk/arm64/release/app-arm64-release.apk
            abi="android"
            case "$apk" in
              */universal/*) abi="android-universal" ;;
              */arm64*/*|*/aarch64*/*) abi="android-arm64-v8a" ;;
              */armeabi*/*|*/arm*/*) abi="android-armeabi-v7a" ;;
              */x86_64*/*) abi="android-x86_64" ;;
              */x86/*) abi="android-x86" ;;
            esac
            out="release/NekoBeat-${VERSION}-${abi}.apk"
            cp -f "$apk" "$out"
            echo "Staged $apk → $out"
          done
          ls -lah release/
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Upload release/ APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: nekobeat-android-apk
          path: release/*.apk
          if-no-files-found: error

      - name: Attach APK to the same GitHub Release (no new page, keep notes)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.prepare-release.outputs.tag }}
        run: |
          set -euo pipefail
          # Upload only — do not edit title/body (softprops was overwriting desktop notes)
          for attempt in 1 2 3 4 5; do
            if gh release upload "$TAG" release/*.apk --clobber; then
              echo "APK attached to release $TAG"
              exit 0
            fi
            echo "Upload attempt $attempt failed; retrying in 15s..."
            sleep 15
          done
          echo "::error::Failed to attach APK to release $TAG after retries"
          exit 1
